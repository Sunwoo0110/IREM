name: Git clone test-0207

on: [push]

jobs:
   git_clone_test:
      runs-on: ubuntu-latest
      name: Git clone test
      steps:
         - name: Checkout
           uses: actions/checkout@v3
         - name: Setup
          #  env: 
          #     GH_TOKEN: ${{ secrets.GH_TOKEN }}
           with:
              github-repo-url: "https://github.com/Sunwoo0110/Node_gitaction_test"
              github-user-id: "Sunwoo0110"
              github-user-email: "Sunwoo0110@g.skku.edu"
              github-user-password: "1234"
              # GH_TOKEN: "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"
              GH_TOKEN: ${{ secrets.GH_TOKEN }}
              
           uses: ./ 
           
         - name: Test finish
           run: echo "test was good"
